name: analyzed_demo_plugin_loader
version: 1.0.0-dev
description: 这是插件使用的依赖关系
environment:
  sdk: ">=2.8.1 <3.0.0"
dependencies:
  analyzer: '^0.39.12'
  path: ^1.7.0
  analyzer_plugin: ^0.3.0

dev_dependencies:
  pedantic: ^1.9.0
  test: ^1.14.4
  #angular_analyzer_plugin: '1.0.0' # 例如

  #  还支持覆盖操作
# === vvv REMOVE WHEN PUBLISHING vvv ===
#dependency_overrides:
#  angular_analyzer_plugin:
#      path: ../../../angular_analyzer_plugin
# === ^^^ REMOVE WHEN PUBLISHING ^^^ ===